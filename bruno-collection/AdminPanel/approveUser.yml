info:
  name: approveUser
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{api_url}}admin/approve/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
