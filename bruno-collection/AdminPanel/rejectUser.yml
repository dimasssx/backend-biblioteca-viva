info:
  name: rejectUser
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{api_url}}admin/reject/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
