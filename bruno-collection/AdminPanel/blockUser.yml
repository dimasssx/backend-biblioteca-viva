info:
  name: blockUser
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{api_url}}admin/block/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
