info:
  name: getAll Reviews
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}admin/reviews"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
