info:
  name: like comment
  type: http
  seq: 5

http:
  method: PUT
  url: "{{api_url}}work/{{work_id}}/comments/{{comment_id}}/like"
  auth: inherit

runtime:
  variables:
    - name: comment_id
      value: c8000003-0000-0000-0000-000000000002

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
