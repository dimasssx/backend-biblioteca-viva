info:
  name: createComment
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url}}work/{{work_id}}/comments"
  body:
    type: json
    data: |-
      {
        "content": "Ótima obra! Gostei muito da abordagem."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
