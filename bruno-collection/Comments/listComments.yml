info:
  name: listComments
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}work/{{work_id}}/comments"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
