info:
  name: editComment
  type: http
  seq: 3

http:
  method: PUT
  url: "{{api_url}}work/{{work_id}}/comments/{{comment_id}}"
  body:
    type: json
    data: |-
      {
        "content": "Comentário editado pelo admin."
      }
  auth: inherit

runtime:
  variables:
    - name: comment_id
      value: c8b708e2-eb1a-4561-ba1a-48539202d79f

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
