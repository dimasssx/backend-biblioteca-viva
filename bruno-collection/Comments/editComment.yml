info:
  name: editComment
  type: http
  seq: 3

http:
  method: PUT
  url: "{{api_url}}work/{{work_id}}/comments/{{comment_id}}"
  body:
    type: json
    data: |-
      {
        "content": "Comentário editado pelo admin."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
