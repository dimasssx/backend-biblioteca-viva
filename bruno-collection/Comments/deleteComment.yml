info:
  name: deleteComment
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{api_url}}work/{{work_id}}/comments/{{comment_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
