info:
  name: subscribe
  type: http
  seq: 7

http:
  method: POST
  url: "{{api_url}}bookclub/{{id}}/subscribe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
