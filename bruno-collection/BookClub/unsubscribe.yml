info:
  name: unsubscribe
  type: http
  seq: 8

http:
  method: POST
  url: "{{api_url}}bookclub/{{id}}/unsubscribe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
