info:
  name: Get By Type
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}work?type=Article"
  params:
    - name: type
      value: Article
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
