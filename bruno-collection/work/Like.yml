info:
  name: Like
  type: http
  seq: 9

http:
  method: PUT
  url: "{{api_url}}work/{{work_id}}/like"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
