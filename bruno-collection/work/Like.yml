info:
  name: Like
  type: http
  seq: 5

http:
  method: POST
  url: "{{api_url}}work/{{work_id}}/like"
  auth: inherit

runtime:
  variables:
    - name: work_id
      value: "9999"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
