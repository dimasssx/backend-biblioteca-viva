info:
  name: Unlike
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{api_url}}work/{{work_id}}/like"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
