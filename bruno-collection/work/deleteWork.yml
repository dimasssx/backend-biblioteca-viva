info:
  name: deleteWork
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{api_url}}work/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
