info:
  name: GetAll
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}work?type="
  params:
    - name: type
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
