info:
  name: GetAll
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}work?type=poem"
  params:
    - name: type
      value: poem
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
