info:
  name: GetAll
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}work?type=infographic"
  params:
    - name: type
      value: infographic
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
