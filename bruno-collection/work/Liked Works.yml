info:
  name: Liked Works
  type: http
  seq: 9

http:
  method: GET
  url: "{{api_url}}work/liked"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
