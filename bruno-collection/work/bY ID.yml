info:
  name: bY ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}work/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: 64d73500-78fc-4635-9d2d-4ba295ec0217
  scripts:
    - type: after-response
      code: bru.setGlobalEnvVar("work_id", res.getBody().id)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
