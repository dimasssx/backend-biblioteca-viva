info:
  name: bY ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}work/99999999-9999-9999-9999-999999999002"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setGlobalEnvVar("work_id", res.getBody().id)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
